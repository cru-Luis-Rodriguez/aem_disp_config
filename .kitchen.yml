---
driver:
  name: vagrant

provisioner:
  name: chef_solo
  environment_path: test/environments
  client.rb:
	enviroment: aem_uat

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      - recipe[aem_disp_config::default]
    attributes:
